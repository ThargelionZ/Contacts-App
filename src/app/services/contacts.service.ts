import { Injectable } from '@angular/core';

@Injectable()
export class ContactsService {

  constructor() { }
  // In order to use the service elsewhere you must use constructor(private contactsService: ContactsService) {}
}
